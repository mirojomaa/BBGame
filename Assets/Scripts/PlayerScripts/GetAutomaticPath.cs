using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using UnityEngine;

public class GetAutomaticPath : MonoBehaviour
{
    /*
    private List<Vector3> Pathpoints = new List<Vector3>();
    private float speedTravelcurve = 5;
    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("CurveStarter"))

        {
            Pathpoints.Clear();
            Pathpoints.AddRange(5);
            for (int i = 0; i < Pathpoints.Count)
            {
                StartCoroutine(movSPeedDuringCurve());
                this.transform.position = Pathpoints[i];
            }
               
        }
    }
    */


 
}

